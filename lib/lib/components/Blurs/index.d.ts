import { ComponentType } from 'react';
type BlursPropsType = {
    currentBlur?: {
        x: number;
        y: number;
    }[];
};
declare const Blurs: ComponentType<BlursPropsType>;
export default Blurs;
