import styled from '@emotion/styled';
import { ComponentType } from 'react';

const WordsInput: ComponentType = () => {
  const WordsInputContainer = styled.div``;
  return <WordsInputContainer>
    
  </WordsInputContainer>
};

export default WordsInput;
